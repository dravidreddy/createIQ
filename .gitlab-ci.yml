stages:
  - test

test:
  stage: test
  image: python:3.11
  before_script:
    - pip install --upgrade pip
    - pip install -r backend/requirements.txt
  script:
    - python -m pytest backend/testing/core/
  only:
    - main
    - merge_requests
